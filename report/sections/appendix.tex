\newpage
\appendix

\section{Supporting Analysis for Scenario Sampling}
\label{app:scenario_sampling}

This appendix provides supporting evidence for the choice to sample the planned duration error in the scenario generation procedure.

The planned duration is compared with the historical average realized duration per surgery type. For both estimators, the sum of absolute errors with respect to the realized duration is computed. The results are shown in Table~\ref{tab:absolute_errors}.

\begin{table}[h]
    \centering
    \caption{Comparison of sum of absolute errors}
    \label{tab:absolute_errors}
    \begin{tabular}{l r}
        \hline
        Estimator & Sum of abs. errors in minutes \\
        \hline
        Planned duration & 333{,}179 \\
        Avg. realized duration per surgery type & 375{,}746.81 \\
        \hline
    \end{tabular}
\end{table}

The sum of absolute errors when using the historical average realized duration per surgery type equals $112.8\%$ of the sum of absolute errors obtained when using the planned duration. Therefore, the error is $12.8\%$ higher when using the historical average realized duration instead of the planned duration.

Figures~\ref{fig:hist_average_realized_error} and~\ref{fig:hist_planned_duration_error} show the corresponding error distributions.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/histogram_average_realized_error.png}
    \caption{Histogram of the prediction error when using the historical average realized duration per surgery type.}
    \label{fig:hist_average_realized_error}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/histogram_planned_duration_error.png}
    \caption{Histogram of the prediction error when using the planned duration.}
    \label{fig:hist_planned_duration_error}
\end{figure}

\section{SAA Convergence for Steps 1 and 2}
\label{app:saa_convergence}

This appendix provides the SAA convergence results for Step 1 (timing only) and Step 2 (sequencing and timing), complementing the Step 3 results discussed in Section~\ref{subsec:SAAresults}. Figures~\ref{fig:saa_convergence_step1} and~\ref{fig:saa_convergence_step2} use the same layout as Figure~\ref{fig:saa_convergence_step3}: the out-of-sample evaluation value is plotted against the number of training scenarios $N$ for each sampling method, with the line the mean over replications and the shaded band spanning $\pm 1$ standard deviation.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/saa_convergence_step1.png}
    \caption{SAA convergence for Step 1 (timing only).}
    \label{fig:saa_convergence_step1}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/saa_convergence_step2.png}
    \caption{SAA convergence for Step 2 (sequencing and timing).}
    \label{fig:saa_convergence_step2}
\end{figure}

\section{Objective Breakdowns for Steps 1 and 2}
\label{app:objective_breakdowns}

This appendix provides the in-sample and out-of-sample objective breakdowns for Step 1 (timing only) and Step 2 (sequencing and timing), complementing the Step 3 breakdown discussed in Section~\ref{subsec:traineval}. Figures~\ref{fig:objective_breakdown_step1} and~\ref{fig:objective_breakdown_step2} show the same components and layout as Figure~\ref{fig:objective_breakdown_step3}: for each waiting list the left bar is the in-sample (training) cost and the right bar the out-of-sample (evaluation) cost, stacked by objective component.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/objective_breakdown_step1.png}
    \caption{Objective breakdown for Step 1 (timing only) on four waiting lists.}
    \label{fig:objective_breakdown_step1}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/objective_breakdown_step2.png}
    \caption{Objective breakdown for Step 2 (sequencing and timing) on four waiting lists.}
    \label{fig:objective_breakdown_step2}
\end{figure}

\section{Value of the Stochastic Solution for Steps 1 and 2}
\label{app:vss_breakdowns}

This appendix reports the out-of-sample EEV and RP for Step 1 (timing only) and Step 2 (sequencing and timing), complementing the Step 3 results discussed in Section~\ref{subsec:VSS}. Figures~\ref{fig:vss_step1} and~\ref{fig:vss_step2} use the same layout as Figure~\ref{fig:vss_step3}: the annotated gap between the deterministic (EV) and stochastic (SAA) bars is the estimated VSS. For both steps the VSS is positive for all four waiting lists.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/vss_eev_rp_step1.png}
    \caption{Out-of-sample EEV and RP for Step 1 (timing only) on four waiting lists.}
    \label{fig:vss_step1}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/vss_eev_rp_step2.png}
    \caption{Out-of-sample EEV and RP for Step 2 (sequencing and timing) on four waiting lists.}
    \label{fig:vss_step2}
\end{figure}

\section{SAA Procedure}
\label{app:saa_alg}

Algorithm~\ref{alg:saa} summarises the Sample Average Approximation procedure
referenced in Section~\ref{sec:results} and used throughout the experiments.

\begin{algorithm}[H]
\caption{Sample Average Approximation (SAA) Algorithm for Stochastic Discrete Optimization}
\label{alg:saa}
\begin{algorithmic}[1:]
\Statex \textbf{Step 1:}
\State Choose initial sample sizes $N$ (nr. of scenarios) and $N'$ (size of the evaluation sample), a decision rule for determining the number $M$ of SAA replications, a decision rule for increasing the sample sizes $N$ and $N'$ if needed, and tolerance $\varepsilon$.
\Statex \textbf{Step 2:}
\For{$m = 1, \dots, M$}
    \State Generate a sample of size $N$ and solve the SAA problem with objective value $\hat{v}_N^m$ and $\varepsilon$-optimal solution $\hat{x}_N^m$.
\State Estimate the optimality gap $g(\hat{x}_N^m) - v^*$ and the variance of the gap estimator.
    \If{the optimality gap and the variance of the gap estimator are sufficiently small}
        \State \textbf{go to Step 4}
    \EndIf
\EndFor
\Statex \textbf{Step 3:}
\If{the optimality gap or the variance of the gap estimator is too large}
    \State Increase the sample sizes $N$ and/or $N'$.
    \State Return to Step 2.
\EndIf
\Statex \textbf{Step 4:}
\State Choose the best solution $\hat{x}$ among all candidate solutions $\hat{x}_N^m$ produced, using a screening and selection procedure.
\State \textbf{Stop.}
\end{algorithmic}
\end{algorithm}

\section{Blueprint Comparison for Steps 1 and 2}
\label{app:bp_comparison}

Figures~\ref{fig:bp_comparison_step1} and~\ref{fig:bp_comparison_step2} report
the baseline-versus-blueprint operational-cost comparison for Step~1 (timing
only) and Step~2 (sequencing and timing), complementing the Step~3 result in
Section~\ref{sec:bp_apply}. For both steps the patient-to-session assignment is
fixed from the documented schedule, so the blueprint linking constraint is
non-binding and the two policies coincide for every comparison seed.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/blueprint_comparison_step1.png}
    \caption{Blueprint vs.\ baseline out-of-sample cost for Step~1 (timing only) across comparison seeds.}
    \label{fig:bp_comparison_step1}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{Figures/blueprint_comparison_step2.png}
    \caption{Blueprint vs.\ baseline out-of-sample cost for Step~2 (sequencing and timing) across comparison seeds.}
    \label{fig:bp_comparison_step2}
\end{figure}

\section{Blueprint Stability Across Sampling Seeds}
\label{app:bp_stability}

Because the blueprint is constructed from a finite SAA of the underlying
uncertainty ($|R|$ waiting-list realisations and $|S|$ duration scenarios per
realisation), a single solve reflects one random draw. To establish that the
recommended quota table $N_{gh}$ is a property of the data rather than an
artefact of one sample, the three-stage program is re-solved over nine
independent seeds $\theta \in \{42, \ldots, 50\}$ with a larger SAA instance
($|R|=5$, $|S|=20$, LHS, $\beta_5 = 1$). The $k$-means clustering of
Section~\ref{sec:clustering} is computed \emph{once} on the full ORT history
with a fixed clustering seed, so $\theta$ governs only the operational draws and
the S/M/L labels remain comparable across runs.

Tables~\ref{tab:bp_seeds_quality} and~\ref{tab:bp_seeds_quotas} summarise the
nine solves. All runs hit the 600-second time limit; the large residual gaps
($\approx$79--84\,\%) sit entirely in the operational cost component, while the
blueprint capacities $N_{gh}$ are at the proven lower bound in every run.

\begin{table}[h]
  \centering
  \caption{Blueprint solution quality across sampling seeds
           ($|R|=5$, $|S|=20$, $\beta_5=1$, LHS, 600-s limit;
           all runs hit the time limit, status~9).
           The optimality gap lies entirely in the operational cost component;
           slot costs are at the proven lower bound in every run.}
  \label{tab:bp_seeds_quality}
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{rrrrr}
    \toprule
    Seed & Gap (\%) & Total slots & Slot cost & In-sample obj. \\
    \midrule
    \textbf{42} & \textbf{80.15} & \textbf{20} & \textbf{20.0} & \textbf{95.73} \\
    43 & 81.84 & 21 & 21.0 & 104.61 \\
    44 & 79.43 & 22 & 22.0 & 106.95 \\
    45 & 83.37 & 22 & 22.0 & 120.25 \\
    46 & 81.79 & 19 & 19.0 & 104.33 \\
    47 & 84.40 & 18 & 18.0 & 115.42 \\
    48 & 83.91 & 20 & 20.0 & 124.30 \\
    49 & 80.74 & 20 & 20.0 & 103.86 \\
    50 & 84.21 & 19 & 19.0 & 113.97 \\
    \midrule
    Mean & 82.21 & 20.1 & 20.1 & 109.94 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[h]
  \centering
  \caption{Blueprint quotas $N_{gh}$ per sampling seed, grouped by subgroup;
           $\Sigma$ is the per-subgroup total $\sum_h N_{gh}$.
           The bold row (seed~42) is the recommended robust blueprint
           (Section~\ref{sec:bp_robust}).}
  \label{tab:bp_seeds_sessions}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{r|rrrr|rrrr|rrrr}
    \toprule
    & \multicolumn{4}{c|}{S (Short)} & \multicolumn{4}{c|}{M (Medium)} & \multicolumn{4}{c}{L (Long)} \\
    Seed & $h{=}0$ & $h{=}1$ & $h{=}2$ & $\Sigma$
         & $h{=}0$ & $h{=}1$ & $h{=}2$ & $\Sigma$
         & $h{=}0$ & $h{=}1$ & $h{=}2$ & $\Sigma$ \\
    \midrule
    \textbf{42} & \textbf{6} & \textbf{5} & \textbf{5} & \textbf{16}
                & \textbf{1} & \textbf{1} & \textbf{1} & \textbf{3}
                & \textbf{1} & \textbf{0} & \textbf{0} & \textbf{1} \\
    43 & 6 & 5 & 5 & 16 & 0 & 2 & 2 & 4 & 1 & 0 & 0 & 1 \\
    44 & 7 & 6 & 5 & 18 & 0 & 1 & 1 & 2 & 2 & 0 & 0 & 2 \\
    45 & 5 & 7 & 7 & 19 & 2 & 1 & 0 & 3 & 0 & 0 & 0 & 0 \\
    46 & 6 & 5 & 5 & 16 & 1 & 1 & 1 & 3 & 0 & 0 & 0 & 0 \\
    47 & 6 & 6 & 5 & 17 & 1 & 0 & 0 & 1 & 0 & 0 & 0 & 0 \\
    48 & 6 & 6 & 5 & 17 & 0 & 1 & 1 & 2 & 1 & 0 & 0 & 1 \\
    49 & 5 & 6 & 5 & 16 & 2 & 1 & 0 & 3 & 1 & 0 & 0 & 1 \\
    50 & 6 & 5 & 6 & 17 & 1 & 1 & 0 & 2 & 0 & 0 & 0 & 0 \\
    \bottomrule
  \end{tabular}
\end{table}